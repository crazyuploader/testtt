name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: downloading script
      run: wget https://raw.githubusercontent.com/crazyuploader/build-scripts/master/cpp.sh
    - name: building
      run: bash cpp.sh
